{
  "compilerOptions": {
    "target": "ES6", // Specifies the JavaScript language version for emitted JavaScript.
    "module": "commonjs", // Specifies the module code generated (CommonJS in this case).
    "rootDir": "./src", // The root folder within your source files.
    "moduleResolution": "Node", // Specifies how TypeScript looks up a file from a given module specifier.
    "outDir": "./dist", // The output folder for all emitted files.
    "esModuleInterop": true, // Emit additional JavaScript to support importing CommonJS modules.
    "forceConsistentCasingInFileNames": true, // Ensures that casing is correct in imports.
    "strict": true, // Enables all strict type-checking options.
    "skipLibCheck": true // Skip type checking all .d.ts files.
  },
  "include": ["src/**/*.ts"], // Specifies the files to be included in the compilation.
  "exclude": ["node_modules"] // Specifies the files to be excluded from the compilation.
}
